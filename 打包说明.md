# PDF 书签添加器 - Windows 打包说明

## 环境要求
- Windows 10/11
- Python 3.8+

## 打包步骤

### 1. 安装依赖
```bash
pip install -r requirements.txt
```

### 2. 运行打包脚本
```bash
python build.py
```

### 3. 输出文件
打包完成后，exe 文件位于：
```
dist/PDF书签添加器.exe
```

## 文件说明
- `app.py` - 主程序（启动本地服务器 + WebView 窗口）
- `build.py` - PyInstaller 打包脚本
- `requirements.txt` - Python 依赖
- `index.html` - 前端页面（会被打包进 exe）

## 使用方式
双击 `PDF书签添加器.exe` 即可运行，无需安装。

## 注意事项
- 首次运行可能需要等待几秒钟加载
- 应用会在本地启动 HTTP 服务器（端口 7881）
- 所有处理都在本地完成，不会上传文件到服务器
